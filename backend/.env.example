# Backend runtime configuration
# Note: Variables have sensible defaults where possible.
# You can start the backend without API keys, but real web research
# works best when a search provider and (optionally) an LLM are configured.

# Human-friendly app name (optional)
APP_NAME="crewAI Research Backend"

# Environment name: development | staging | production (optional)
ENVIRONMENT=development

# API base prefix (optional)
API_PREFIX=/api/v1

# Allowed CORS origins for the frontend in development (JSON array string)
# Example for Next.js dev server:
CORS_ORIGINS=["http://localhost:3000"]

# --- Search Providers (choose one and set key) ---
# Supported: tavily | bing | serpapi | ddg (no-key fallback)
SEARCH_PROVIDER=tavily

# Tavily Search
TAVILY_API_KEY=

# Bing Web Search (Azure Cognitive Search)
BING_SEARCH_KEY=
BING_SEARCH_ENDPOINT=

# SerpAPI (Google/Bing wrapper)
SERPAPI_API_KEY=

# --- Content Fetching / Scraping (optional) ---
# Some sites block direct requests; a proxy improves extraction reliability.
# Configure one if available.
SCRAPINGBEE_API_KEY=
BROWSERLESS_API_KEY=
FIRECRAWL_API_KEY=

# --- LLM / Summarization (optional but recommended) ---
# Choose one provider for higher-quality synthesis beyond simple heuristics.
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Azure OpenAI
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=

# Anthropic
ANTHROPIC_API_KEY=

# --- Telemetry / Misc (optional) ---
SENTRY_DSN=
OTEL_EXPORTER_OTLP_ENDPOINT=
